# Disable Button in Queue

in queue, add a button to disable this note from coming up in queue view.
For this, set notes:false (support this in the spec/schema).

the point is to get notes that will never receive comments from stop annoying the user
