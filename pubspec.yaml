name: note
description: "minimal personal notes."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.3
  file_picker: ^10.3.10
  shared_preferences: ^2.5.5
  path: ^1.9.1
  pasteboard: ^0.5.0
  flutter_markdown_plus: ^1.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
